/**
 * Step-1 placeholder shell. The router + pages (Login → Chat → Documents →
 * Audit → Eval) are wired in Step 11 (CLAUDE.md §6.18).
 */
export default function App(): JSX.Element {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50 text-slate-900">
      <div className="text-center space-y-2">
        <h1 className="text-2xl font-semibold">Aegis RAG</h1>
        <p className="text-sm text-slate-500">
          Air-gapped Sovereign RAG — scaffold ready. UI lands in Step 11.
        </p>
      </div>
    </main>
  );
}
