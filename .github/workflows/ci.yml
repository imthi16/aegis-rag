name: CI

on:
  push:
    branches: ["**"]
  pull_request:

# Offline-by-env, even in CI, to catch any accidental hub/telemetry calls.
env:
  HF_HUB_OFFLINE: "1"
  TRANSFORMERS_OFFLINE: "1"
  HF_DATASETS_OFFLINE: "1"

jobs:
  backend:
    name: Backend — lint, type-check, test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: aegis_app
          POSTGRES_PASSWORD: ci_password
          POSTGRES_DB: aegis
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U aegis_app -d aegis"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      APP_ENV: test
      DATABASE_URL: postgresql+asyncpg://aegis_app:ci_password@localhost:5432/aegis
      TEST_DATABASE_URL: postgresql+asyncpg://aegis_app:ci_password@localhost:5432/aegis
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e ".[dev]"

      - name: Ruff (lint)
        run: ruff check .

      - name: Ruff (format check)
        run: ruff format --check .

      # The backend steps above run from ./backend, so repo-root Python (the CI
      # eval gate, provisioning scripts) would otherwise never be linted.
      - name: Ruff (repo-root scripts)
        working-directory: .
        run: |
          ruff check --config backend/pyproject.toml eval infra/scripts
          ruff format --check --config backend/pyproject.toml eval infra/scripts

      - name: mypy (strict)
        run: mypy app

      - name: Alembic upgrade head (verify migration applies)
        run: alembic upgrade head

      - name: pytest
        run: pytest

      # Eval gate runs against LOCAL Ollama + local embeddings ONLY (never
      # OpenAI). Without a model provisioned in the runner it SKIPS (exit 0);
      # provision Ollama + weights and add --require to make it blocking.
      - name: CI eval gate (local models only)
        working-directory: .
        run: python eval/ci_gate.py --suite both

  frontend:
    name: Frontend — strict type-check, build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      # §10 (Frontend) requires `tsc --noEmit` clean and a succeeding build.
      # `npm run build` runs tsc first, but type errors are checked separately
      # so a failure names the actual cause instead of "build failed".
      - name: TypeScript (strict)
        run: npm run typecheck

      - name: Build
        run: npm run build
